const logger = () => {
// Add your middleware Logger on index.js file  to log every request's METHOD, URL, TIME
// e.g if I make a request to get all users using localhost:3000/users at exactly 2pm, it should log like below
// 2026-01-31T14:00:00.123Z  GET    /users
}

export default logger;